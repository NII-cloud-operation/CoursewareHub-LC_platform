#!/bin/bash

source "$(dirname $(readlink -f "$0"))/bashsteps-bash-utils-jan2017.source" || exit

new_dir="$1"
nodecount="$3"

: "${nodecount:=2}"

"$ORGCODEDIR/build-jh-environment.sh-new" "$new_dir"
iferr_exit

"$ORGCODEDIR/kvm-vm-setup.sh-new" "$new_dir" /home/triggers/ubuntu-image-resources/ubuntu-14-instance-build.img-sshkeys-update-upgrade.tar.gz
